# GENERATED FILE — DO NOT EDIT. This file is overwritten on every protolake build.
# Remote cache configuration for Bazel builds
# Generated by Proto Lake from lake.yaml remote_cache settings.
# Loaded via try-import in .bazelrc — safe to delete if not needed.
#
# Activate with: PROTOLAKE_BAZEL_CONFIG=ci (or any profile name defined below)

{#if remoteCacheEnabled}
# Remote cache: {remoteCacheProvider}
build:ci --remote_cache={remoteCacheUrl}
{#if remoteCacheProvider == "gcs"}
build:ci --google_default_credentials
{/if}
{#if remoteCacheCompression}
build:ci --remote_cache_compression
{/if}
build:ci --remote_upload_local_results=true
{#else}
# Remote cache is not configured.
# To enable, add a remote_cache section to your lake.yaml:
#
#   config:
#     remote_cache:
#       provider: "gcs"
#       bucket: "my-project-bazel-cache"
#       compression: true
{/if}
